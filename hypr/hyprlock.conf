...existing code...
background {
    # Use the generated file from the script so the image is chosen at lock time
    image = "/home/glup/.config/hypr/hyprlock/current_wallpaper.jpg"
    # optional: overlay color/alpha to dim the image (example)
    # overlay = 0,0,0,0.25
    # optional: blur = 8
}
...existing code...